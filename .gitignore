# Python
.venv/
__pycache__/
*.pyc

# Data (images, DB, cached embeddings — not committed, regenerable via scripts)
data/images/*
!data/images/.gitkeep
data/cache/*
!data/cache/.gitkeep
data/*.db
data/*.npy

# Env
.env

# Node (ui, later phases)
node_modules/
.next/
dist/

# OS
.DS_Store
